# Pinned rather than following compileSdk. Robolectric ships a separate Android runtime
# per API level, and the newest one is always the least exercised — a test suite that
# breaks when compileSdk moves is a suite that gets disabled. 34 is broadly supported and
# far above this app's minSdk of 26.
sdk=34
