# Pinned rather than following compileSdk, for the same reason as the feature modules:
# Robolectric ships a separate Android runtime per API level and the newest is always the
# least exercised. 34 is broadly supported and far above this app's minSdk of 26.
sdk=34
